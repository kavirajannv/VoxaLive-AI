{
  "name": "global-voice-translator",
  "version": "1.0.0",
  "description": "Global Voice Translator AI — Production Runner",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && node server.js",
    "prod": "cd frontend && npm run build && cd ../backend && npx cross-env NODE_ENV=production node server.js"
  }
}
