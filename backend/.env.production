# Production Environment
NODE_ENV=production
PORT=5000

# Database (use your production MongoDB URI)
MONGODB_URI=mongodb

# Auth (IMPORTANT: change this to a strong random secret!)
JWT_SECRET=global

# Frontend URL (set to your domain in production, or leave empty if serving from same origin)
# FRONTEND_URL=https://yourdomain.com
